# PyTorch is intentionally absent: preserve the user's CUDA-specific install.
numpy==2.4.4
transformers==5.14.1
safetensors==0.8.0
huggingface_hub==1.24.0
soxr==1.1.0
librosa==0.11.0
# Cohere Transcribe loads through its transformers-native runtime (device_map +
# sentencepiece tokenizer), which needs accelerate and sentencepiece at runtime.
accelerate==1.14.0
sentencepiece==0.2.2
# Optional 8-bit (int8) loading of the Cohere model halves VRAM usage.
bitsandbytes==0.50.0
